# v1.3.2 — Native Update Interaction

This patch release moves the macOS update experience entirely into Sparkle's standard interface.

- Keeps automatic checks silent when no update is available.
- Shows manual results and available releases in native macOS windows.
- Keeps release notes, skip/install choices, progress, installation, and relaunch in one flow.
