# language: zh-CN
@canvas @v2.0.0
功能: 用户以只读方式查看画布
  只读画布统一由 CanvasReadOnlyView 提供（引用卡内嵌预览、提案预览、artifact 缩略、对话引用）。

  @P0 @happy_path @CV-X6-RO-001
  场景: 只读画布禁止编辑交互
    假如用户正在查看一张只读画布
    当用户尝试拖动其中的节点
    那么节点位置不应改变（拖动 / 连线 / 打组 / 缩放禁用）

  @P0 @happy_path @CV-X6-RO-002
  场景: 只读画布保留平移与缩放查看
    假如用户正在查看一张只读画布
    当用户滚动滚轮
    那么只读画布应能缩放（平移与缩放查看可用）

  @P0 @happy_path @CV-X6-RO-003
  场景: 各只读入口共用同一渲染
    假如画布上有一张引用卡
    那么卡内内嵌预览应渲染目标画布节点（与对话引用的只读画布同一组件）