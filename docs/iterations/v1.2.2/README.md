# v1.2.2 — Agent 上下文压缩与对话管理

本次 patch 版本补齐了 Agent 长对话的上下文压缩能力，并完善了对话列表操作与排序体验。

- 支持自动和手动压缩 Agent 对话上下文，并展示压缩进度和可恢复的压缩记录。
- Entity Catalog 只在请求模型时按当前分支投影，不再把重复 Catalog 持久化进历史消息。
- 对话列表右键菜单提供与对话详情一致的完整操作。
- 修复可拖拽分隔条占用额外布局宽度的问题，同时保留宽松的透明拖拽区域。
- 修复从旧回复 Fork 后新对话未稳定排到列表顶部的问题，刷新或重启后顺序仍保持正确。
