# language: zh-CN
@canvas @v2.0.0
功能: 用户在画布上建立并调整连线
  连线创建、样式与标签编辑走 X6 连接规则（allowMulti / createEdge）与边工具栏。

  @P0 @happy_path @CV-X6-EDGE-001
  场景: 用户从出桩拖到入桩建立有向边
    假如画布上有两个文本节点
    当用户从第一个节点的出桩拖到第二个节点的入桩
    那么画布上应出现一条有向边，由第一个节点指向第二个节点

  @P0 @happy_path @CV-X6-EDGE-002
  场景: 用户建立同源同目标的平行边
    假如画布上已有两个节点
    当用户重复两次从同一出桩拖到同一入桩
    那么应存在两条平行边（允许重复连接）

  @P0 @happy_path @CV-X6-EDGE-003
  场景: 新连线自带画布归属与默认样式
    假如用户刚连好一条新边
    那么这条边应属于当前画布且带默认样式

  @P0 @happy_path @CV-X6-EDGE-004
  场景: 用户调整连线样式并保留
    假如画布上有一条选中的边
    当用户在边工具栏中依次修改颜色、形状、线型、线宽与箭头样式
    那么边应立即呈现新样式
    且用户重新进入画布后样式仍保留

  @P0 @happy_path @CV-X6-EDGE-005
  场景: 用户双击边标签编辑并提交
    假如画布上有一条选中的边
    当用户双击边上的标签输入文字并提交
    那么边应显示该标签
    且用户重新进入画布后标签仍保留

  @P0 @happy_path @CV-X6-EDGE-006
  场景: 用户清空边标签回到无标签
    假如画布上有一条带标签的边
    当用户双击标签并清空内容后提交
    那么边应回到无标签状态

  @P0 @happy_path @CV-X6-EDGE-007
  场景: 用户从边工具栏删除选中边
    假如画布上有一条选中的边
    当用户点击边工具栏的删除
    那么该边应从画布上消失

  @P0 @happy_path @CV-X6-EDGE-008
  场景: 用户选择的连接端口在重新进入后保持
    假如画布上有上下错开的两个文本节点
    当用户从第一个节点的下方出桩连接到第二个节点的上方入桩
    那么连线应从下方出桩进入上方入桩
    而且用户重新进入画布后连线端口仍然保持
