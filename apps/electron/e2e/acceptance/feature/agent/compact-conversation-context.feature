# language: zh-CN
@agent @v1.2.2
功能: 用户压缩过长的 Agent 对话上下文
  用户需要主动整理过长的对话，并且能够检查压缩结果。

  @P0 @happy_path @AG-COMPACT-001
  场景: 过长对话在继续回复前自动压缩上下文
    假如当前对话已经接近模型上下文上限
    当用户继续发送消息
    那么页面应该显示正在压缩上下文
    而且压缩完成后 Agent 应该继续处理用户消息
    而且对话中应该显示一条压缩回执

  @P0 @control @AG-COMPACT-002
  场景: 用户手动压缩当前对话上下文
    假如当前对话已有足够长的历史消息
    当用户从对话操作菜单选择压缩上下文
    那么页面应该显示正在压缩上下文
    而且压缩期间发送操作和压缩操作应该显示为不可用
    而且完成后应该显示一条压缩回执

  @P0 @visibility @AG-COMPACT-003
  场景: 用户检查压缩后的上下文检查点
    假如当前对话已经完成一次上下文压缩
    那么用户应该看到压缩回执和压缩前后的 token 估计
    当用户展开压缩回执
    那么用户应该看到压缩后的上下文检查点
    而且原始用户消息和 Agent 回复应该仍然可见

  @P0 @recovery @AG-COMPACT-004
  场景: 用户重启应用后仍能检查压缩回执
    假如当前对话已经完成一次上下文压缩
    当用户关闭并重新打开 Reflecta 应用
    而且用户打开原对话
    那么压缩回执应该仍然存在
    而且用户应该仍能展开查看上下文检查点

  @P1 @error @AG-COMPACT-005
  场景: 当前对话过短时用户得到清晰反馈
    假如当前对话只有一轮较短的消息
    当用户从对话操作菜单选择压缩上下文
    那么页面应该说明当前对话还不需要压缩
    而且用户应该可以继续输入消息
