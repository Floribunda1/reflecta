# language: zh-CN
@regression @agent
功能: 重新生成回复期间用户消息保持可见
  重新生成（或编辑重发）历史回复时，主进程会先把会话投影截断到分支点，再创建模型运行时，
  随后重新发布本轮的用户消息。回归保护的是：从点击重新生成到新回复出现的整个过程中，
  本轮用户消息必须在界面上持续可见，不能出现先消失、后重新出现的闪烁。

  场景: 重新生成回复时本轮用户消息始终保持可见
    假如对话中已经有两轮完整的问答
    当用户对最后一条 Agent 回复执行重新生成
    那么从点击操作到新回复出现的整个过程中，该轮用户消息应该始终可见
    而且新回复完成后，消息顺序应该保持用户消息在前、Agent 回复在后
