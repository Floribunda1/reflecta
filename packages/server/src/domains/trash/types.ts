export type TrashedUnderstandingDTO = {
  id: string;
  title: string | null;
  body: string;
  deletedAt: string;
};

export type TrashedCanvasDTO = {
  id: string;
  title: string;
  deletedAt: string;
};
