# language: zh-CN
@canvas @v2.0.0
功能: 用户以只读方式查看画布
  只读画布统一由 CanvasReadOnlyView 提供（引用卡内嵌预览、提案预览、artifact 缩略、对话引用）。
  对话中点击画布引用打开只读对话框，不占用右侧详情面板。

  @P0 @happy_path @CV-RO-001
  场景: 只读画布禁止编辑交互
    假如用户正在查看一张只读画布
    当用户尝试拖动其中的节点
    那么节点位置不应改变（拖动 / 连线 / 打组 / 缩放禁用）

  @P0 @happy_path @CV-RO-002
  场景: 只读画布保留平移与缩放查看
    假如用户正在查看一张只读画布
    当用户按住 ⌘（Mac 控制键）滚动滚轮
    那么只读画布应能缩放（滚轮平移与缩放查看均可）

  @P0 @happy_path @CV-RO-003
  场景: 各只读入口共用同一渲染
    假如画布上有一张引用卡
    那么卡内内嵌预览应渲染目标画布节点（与对话引用的只读画布同一组件）

  @P0 @happy_path @CV-RO-004
  场景: canvas_present 展示默认折叠且可单张展开
    假如对话中展示了一张包含多张理解卡的只读分析画布
    那么各理解卡首次应默认折叠（只显示完整标题与展开按钮）
    当用户点击其中一张卡片的展开按钮
    那么只有该卡片展开显示正文，其余卡片保持折叠