# v1.1.5

Patch release for the Agent response waiting state.

- Show the existing in-chat "正在思考" placeholder while a run is active and the assistant has not emitted its first response block yet.
- Keep the message list pinned to the bottom when that pending state appears.
