# v1.1.7

Patch release for Agent thread header actions, Markdown export, and per-response branching.

- Add an inline editable Agent thread title bar with consolidated actions in the top header.
- Export Agent conversations to Markdown with only user prompts and Agent replies.
- Move fork from thread duplication to assistant-response branching.
- Align the Agent thread header with the detail inspector header.
