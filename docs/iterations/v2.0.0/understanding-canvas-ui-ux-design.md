# v2.0.0 理解画布 UI/UX 设计

> 日期：2026-08-15
>
> 状态：**讨论中**（信息架构与导航结构性讨论已展开；视觉设计未开始）
>
> 职责：定义理解画布模块与 Agent 协作的 UX / UI 形态（画布工作区、单面板、draft-preview 内联块、artifact panel、就地 Chat 等交互与视觉）
>
> 前置：PRD（产品要求）、服务端设计（接口契约）、共识记录（决策依据）

---

## 0. 信息架构与导航（结构性讨论，视觉设计之前）

> 原则（用户定）：先 UX 与信息架构，视觉后置。本节记录基于现有代码结构的事实 + 变化点 + 待定决策；逐项确认后再展开视觉。
>
> 现有应用结构事实：路由 2 个顶层模块 `/capture`（笔记）+ `/agent`（AI 对话），`/contemplate` 为历史重定向；唯一导航组件是底部 `AppChromeMenu`（二元切换：`pathname.startsWith("/agent") ? capture : agent`）+ 设置齿轮（modal）；Capture = 左 Domain 树 + 主区列表/详情；Chat = 左 ThreadSidebar + 主区线程。

### 0.1 整体 Navigation / Module 逻辑（变化小但有一处必改）

- **路由 2 → 3**：新增 `/understanding-canvas`；`/capture`、`/agent` 不动。
- **AppChromeMenu 二元逻辑失效（导航层唯一必改组件）**：现在是「当前模块取反」，三个模块时无取反可言，必须改为三段切换。
- **新概念——带参跳转（待定）**：M6-6（理解详情 → 跳转画布模块并打开指定画布·编辑模式）与 C2（canvas_ref 元素点击跳转目标画布）都要求「从 A 模块跳到 B 模块并定位到具体对象」；应用目前无此先例（AppChromeMenu 只做无参模块切换）。待定：机制（路由 query/hash / 全局跳转命令）与跨模块返回路径。

### 0.2 画布模块内部 IA（骨架已定 C1，三个结构问题待定）

**已定骨架（C1）**：左栏画布列表（新建/重命名/删除，按更新时间）+ 主区无限画布（顶部极简工具栏 + 左下缩放控制 + 右下 minimap + Cmd/Ctrl+F 搜索浮层）+ 右侧单面板（库模式 / 详情模式互斥）。

**与 Capture 的 IA 对照**：

|      | Capture             | 画布模块                   |
| ---- | ------------------- | -------------------------- |
| 左栏 | Domain 树（导航树） | 画布列表（资源列表）       |
| 主区 | 理解列表 + 详情     | 无限画布（主区即对象本身） |
| 右侧 | 详情面板            | 单面板（库/详情互斥）      |

**待定结构问题**：

1. **左栏语义不同**：Capture 左栏是领域树（归类导航），画布左栏是资源列表——模块切换时用户对「左侧是什么」的心智模型要切换。画布列表是否需要分组/筛选（画布多时怎么办）？
2. **画布网络**：canvas_ref 让画布成为有向图（画布→画布）；目前只有画布内引用元素，画布列表层无关联展示。v1 是否只在元素层做跳转（C2 已定），列表层不做任何画布间导航？
3. **组嵌套深度**：组可嵌套（C2）——嵌套是空间视觉（容器套容器），不引入树；**画布内部 IA = 空间 + 组层级，不是树**（定死，避免过度设计）。嵌套多深是 UX 问题（拖拽目标、框选递归语义），后置。

### 0.3 Agent 侧 IA 联动（四处）

| #   | 联动点                               | 现状                                                                                           | 变化                                                                                                                               |
| --- | ------------------------------------ | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| a   | 实体体系 3 → 4                       | AgentContextRef = understanding/context/domain；@-mention composer 搜 3 类；citation 渲染 3 类 | 加 canvas。链路：composer 搜索 → entity catalog → citation → 点击（Modal 非路由）。**复用 understanding 的既有模板路径，不单独造** |
| b   | 工具活动分组                         | agent-turn-view groupType 按工具名归类；canvas_* 现在落 "other"                                | 加 canvas 分组                                                                                                                     |
| c   | widget 机制（U1）                    | 消息流 = 文本 + 工具活动块                                                                     | 新层级：widget 卡（canvas-draft 只读渲染）；**消息流 IA 多一层**                                                                   |
| d   | artifact panel（U2）+ 沉淀入口（U4） | 对话无产出聚合                                                                                 | 对话 IA 新区域；「应用 draft」= 沉淀动作落点                                                                                       |

**联动设计原则**：a = 类型扩展（复用既有路径）；c = 渲染层级扩展（widget 注册表）；d = 对话 IA 新增区域（形态待调研）；b 最小（分组映射）。

### 0.4 其他联动影响

| 联动点                  | 影响                                                                            | 状态                    |
| ----------------------- | ------------------------------------------------------------------------------- | ----------------------- |
| Capture 理解详情        | M6-6 加「出现于 N 张画布」区块 + 跳转动作                                       | 后端 DTO 已就绪，纯前端 |
| 理解删除 ↔ 画布占位     | 软删/硬删后画布理解卡占位（C2）；恢复理解后卡片是否自动复活未定义               | 待定                    |
| 全局搜索                | 画布不进检索索引；v1 只有画布内搜索                                             | 已定，记演进项          |
| 回收站                  | 画布硬删不进回收站（C1）；与理解删除联动见上                                    | 已定                    |
| 设置                    | 画布数据同 DB 无新设置；PNG 导出走系统对话框                                    | 无影响                  |
| Capture 内联 agent dock | 其 composer 的 @ 实体搜索加不加 canvas——v1 建议不进（@-canvas 非 capture 场景） | 待定                    |
| E2E / feature           | 新模块验收 feature 文件                                                         | 实现期                  |

### 0.5 三个骨架决策（建议先定，其余在其上展开）

1. **导航三元化**：底栏「一个切换按钮」→ 三段切换。顺序建议 笔记 → 画布 → AI 对话（素材 → 结构 → 协作的心智流）。
2. **带参跳转机制**：跨模块定位（M6-6、canvas_ref）需要统一机制——导航层唯一新能力。
3. **Agent 实体第 4 类**：canvas 进 AgentContextRef / composer / citation，复用 understanding 的既有模板，不做例外。

---

## 待办（承接共识，UI/UX 归口）

> 以下为共识文档标记归口本层的待办；本文档步骤内解决并展开为具体设计。

### U1 Inline Widget 机制（消息内联富组件，头号待办）

- **来源**：C15 画布展示 tool 的渲染形态讨论——用户明确要"Inline Widget 的感觉（如天气卡片）"，非代码块转渲染。
- **已定约束**：
  - 消息 part / 工具结果携带 `widgetType` + 结构化 payload；聊天渲染器按 **widget 注册表**（`{ widgetType → React 渲染器 }`）路由到自定义富组件；
  - 这是**聊天渲染基建**（不仅画布——天气卡、理解卡等任何工具可用），非画布专属；
  - 首个 widget：`canvas-draft`（画布草稿预览，只读画布卡，三用组件之一）；
  - 数据契约见 Server 文档 §6.2（CanvasDocument payload）。
- **待设计**：注册表的形态与扩展方式（新增 widget 的成本）、widget 的布局 / 密度 / 与消息流其它元素的层级、加载 / 失败 / 流式状态、是否支持交互（如画布 widget 内的"应用 / 修改 / 拒绝"）。
- **待调研**：现有聊天渲染器（agent-turn-view / AgentToolActivityView）改造点；Generative UI 模式（Vercel AI SDK genUI、Anthropic show_widget）的可借鉴形态。

### U2 artifact panel 的 UX/UI 形态（C14，待调研）

- **来源**：C14 成果可见性。
- **已定约束**：只聚合本对话**已落地**的产出（approve 并保存的 understanding / 新建 context / sketch 应用后的 canvas）；pending 提案留在消息流，panel 只显示已落地；随 v1 发布。
- **待设计**：面板形态（侧栏 / 底部 / 折叠）、列表内容与排序、点击跳转行为、与消息流 proposal 卡的视觉区分。
- **待调研**：同类产品（对话产出聚合）的 UX 先例。

### U3 画布侧就地 Chat 的 UX（C7 入口二，v1.x）

- **来源**：C7 协作主张「两种协作入口并存」——画布侧就地协作：画布里打开 Chat 侧边栏，AI 以当前画布为作用域，边看画布边聊边组织。
- **已定约束**：与全局 Chat 形态同构；AI 自动读当前画布（scope 注入，无需手动贴 id）。
- **待设计**：侧边栏形态、与右侧单面板（库/详情）的关系、选中元素与对话的联动。
- **版本**：v1.x（v1 只保证对话侧沉淀入口）。

### U4 沉淀入口的 UX（对话 → 画布）

- **来源**：C7 协作主张「对话侧沉淀」+ C14 成果可见性。
- **已定约束**：对话中形成的结构可一键沉淀为画布（或更新画布）；低摩擦（不是用户主动想起来"画个图"）。
- **待设计**：入口位置与触发时机、与 M8-5 draft-preview 的关系（沉淀 = draft 应用？）。

### U5 draft-preview 内联块的 UX（M8-5 / C15，并入 U1 widget 机制）

- **来源**：C15 画布展示 tool。
- **已定约束**：Agent 消息内联渲染只读小画布（mermaid 式），用户在此诊断后「应用 / 修改 / 拒绝」。
- **待设计**：内联块的形态（大小 / 缩放 / 展开）、「应用 / 修改 / 拒绝」交互、与应用后反馈。

### U6 其他待 UI/UX 步骤检视的已定项

- M6-6 画布归属在理解详情的展示与跳转（已定跳转画布模块打开编辑模式）。
- M8-6 引用 `[[cv:]]` 的只读 Modal 形态（与 draft-preview 共用渲染组件）。
- 理解列表「N 条引用」文案（TBD-3 修正，已定）。

## 关联文档

- PRD：`understanding-canvas-prd.md`（模块二、五、六、八）
- 服务端设计：`understanding-canvas-server-design.md`
- 共识记录：`understanding-canvas-consensus.md`（C7 / C14 / C15）
