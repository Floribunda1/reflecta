# v1.1.6

Patch release for Agent chat editing and proposal readability.

- Keep pasted Markdown in the Agent composer as plain editable text instead of rich formatting.
- Refine the collapsed Agent chat jump navigation state.
- Render proposal references as readable labels and include domain paths for Understanding proposals.
