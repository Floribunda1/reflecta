# v2.0.7

## AI reasoning level

Fixed reasoning-level clamping to use pi's remote model catalog, so remote-only models such as `deepseek-v4.1-flash` keep their selected thinking level instead of resetting to `off`.
