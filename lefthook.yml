# pre-commit:
#   commands:
#     check:
#       glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
#       run: bunx oxlint --fix && bunx oxfmt --no-error-on-unmatched-pattern && bun typecheck
#       stage_fixed: true

# pre-push:
#   commands:
#     check:
#       glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
#       run: bunx oxlint && bunx oxfmt --check && bun typecheck
#     typecheck:
#       run: bun run typecheck
