# language: zh-CN
@canvas @v2.0.0
功能: 用户重新进入画布后内容保持一致
  位置 / 尺寸 / 组结构 / 边集合 / 组渲染层级在保存与重载往返中保持不变；编辑中的
  未保存状态对用户可见并在自动保存后回到已保存。

  @P0 @happy_path @CV-PERSIST-001
  场景: 节点位置与尺寸重载一致
    假如用户调整过节点位置或尺寸
    当用户离开并重新进入画布
    那么节点的位置与尺寸应与离开前一致

  @P0 @happy_path @CV-PERSIST-002
  场景: 组的层级与相对坐标重载一致
    假如画布上存在组结构
    当用户离开并重新进入画布
    那么组与成员的父子关系及相对坐标应保持一致

  @P0 @happy_path @CV-PERSIST-003
  场景: 平行边重载不去重、不丢失
    假如画布上有两条平行边
    当用户离开并重新进入画布
    那么两条边都应保留

  @P0 @happy_path @CV-PERSIST-004
  场景: 打组后保存重进仍是组结构
    假如用户刚把节点打成一个组
    当用户离开并重新进入画布
    那么这些节点应仍处于组内

  @P0 @happy_path @CV-PERSIST-005
  场景: 组背景渲染层级重载一致
    假如画布上有一个组（含成员）
    当用户离开并重新进入画布
    那么组背景仍应渲染在成员之下（成员覆盖组背景）

  @P0 @happy_path @CV-PERSIST-006
  场景: 用户修改后看到未保存提示并自动保存
    假如用户已进入一张画布
    当用户在画布上做出修改
    那么画布上应出现未保存的提示
    且稍等片刻后提示消失（修改已自动保存）