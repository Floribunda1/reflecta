# language: zh-CN
@agent @v1.2.2
功能: 用户整理过长的 Agent 对话上下文
  用户需要在对话变长后继续自然交流，并且能够知道上下文何时被压缩、检查压缩结果。

  @P0 @automatic @AG-COMPACT-001
  场景: 对话接近上下文上限时自动压缩
    假如当前 Agent 对话接近所选模型的上下文上限
    当用户继续发送消息
    那么系统应该先压缩较早的对话上下文
    而且最近的对话内容应该继续以原文参与回复
    而且用户原来的消息不应该从对话中消失

  @P0 @manual @AG-COMPACT-002
  场景: 用户手动压缩当前对话上下文
    假如用户已经完成至少一轮 Agent 对话
    当用户从对话操作菜单选择压缩上下文
    那么页面应该显示正在压缩上下文
    而且压缩期间不应该允许再次发送消息或重复压缩
    而且完成后应该显示一条压缩回执

  @P0 @visibility @AG-COMPACT-003
  场景: 用户检查压缩后的上下文检查点
    假如当前对话已经完成一次上下文压缩
    那么用户应该看到压缩回执和压缩前后的 token 估计
    当用户展开压缩回执
    那么用户应该看到压缩后的上下文检查点
    而且原始用户消息和 Agent 回复应该仍然可见

  @P0 @recovery @AG-COMPACT-004
  场景: 用户重启应用后仍能检查压缩回执
    假如当前对话已经完成一次上下文压缩
    当用户关闭并重新打开 Reflecta 应用
    而且用户打开原对话
    那么压缩回执应该仍然存在
    而且用户应该仍能展开查看上下文检查点

  @P1 @branching @AG-COMPACT-005
  场景: 用户编辑旧消息后只保留当前分支的压缩历史
    假如一次上下文压缩发生在某条历史用户消息之后
    当用户编辑该历史消息并重新发送
    那么旧分支后续的压缩回执不应该出现在当前对话
    而且新分支应该根据自己的当前历史继续判断是否需要压缩
