# language: zh-CN
@canvas @v2.0.0
功能: 用户选择内容并执行画布命令
  单选 / 多选 / 框选由 X6 Selection 插件与显式点击接线提供；右键菜单承载
  复制、重复、置顶置底、解组、删除与空白粘贴等命令。
  X6 冲突规则：左键同时承担框选与空间平移会整体禁用 panning，因此平移取舍为中键拖拽、
  Space 平移不再提供（与左键框选互斥，产品已确认）。

  @P0 @happy_path @CV-SEL-001
  场景: 用户单击选中节点出现操作条
    假如画布上有一个节点
    当用户单击该节点
    那么节点应出现选中态与操作条，且不出现选区工具条

  @P0 @happy_path @CV-SEL-002
  场景: 用户单击选中边出现底部边工具栏
    假如画布上有一条边
    当用户单击该边
    那么画布底部应出现边工具栏

  @P0 @happy_path @CV-SEL-003
  场景: 用户 ⌘/Ctrl 加点击多选节点
    假如画布上有两个节点
    当用户单击选中第一个，再按 ⌘/Ctrl 点击第二个
    那么两个节点都应处于选中态
    且不显示各节点操作条、改为显示统一的选区工具条

  @P0 @happy_path @CV-SEL-004
  场景: 用户在空白处拖拽框选多个节点
    假如画布上有两个节点
    当用户在空白处按住左键拖出框住两个节点的矩形
    那么两个节点都应被选中并出现选区工具条

  @P0 @happy_path @CV-SEL-005
  场景: 用户点击空白清空选区
    假如画布上已有选中节点
    当用户点击空白处
    那么选区应被清空且选区工具条消失

  @P0 @happy_path @CV-SEL-006
  场景: 用户用选区工具条删除多选节点且可撤销
    假如画布上有两个被选中的节点
    当用户点击选区工具条的删除
    那么两个节点都应从画布上消失
    且按撤销快捷键后两个节点都恢复

  @P0 @happy_path @CV-SEL-007
  场景: 节点右键菜单提供组织与删除操作
    假如画布上有一个选中的节点
    当用户右键该节点
    那么应出现右键菜单
    且菜单包含复制、重复、置顶、置底与删除

  @P0 @happy_path @CV-SEL-008
  场景: 用户右键粘贴在指针位置
    假如用户已复制画布上的一个节点
    当用户在空白处右键并选择「粘贴」
    那么应出现被复制节点的副本
    且副本落在右键点击的位置

  @P0 @happy_path @CV-SEL-009
  场景: 用户边右键菜单删除连线
    假如画布上有一条边
    当用户右键该边并选择「删除连线」
    那么该边应从画布上消失

  @P0 @happy_path @CV-SEL-010
  场景: 用户通过右键「重复」复制节点
    假如画布上有一个选中的节点
    当用户右键该节点并选择「重复」
    那么画布上应出现与该节点内容一致的副本

  @P0 @happy_path @CV-SEL-011
  场景: 用户置顶 / 置底节点调整叠放顺序
    假如画布上有两个相互重叠的节点
    当用户右键下层节点并选择「置顶」
    那么该节点应显示在另一节点之上
    且对上层节点选择「置底」后恢复原来的叠放顺序